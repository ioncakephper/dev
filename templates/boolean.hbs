  <div class="form-check">
    <label class="form-check-label" for="{{{name}}}" id="{{{name}}}-label" name="{{{name}}}-label">
      <input class="form-check-input" type="checkbox" id="{{{name}}}" name="{{{name}}}"{{#if checked}} checked{{/if}} /> {{{label}}}
    </label>
  </div>